apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-app-deployment
  labels:
    app: flask-microservice
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flask-microservice
  template:
    metadata:
      labels:
        app: flask-microservice
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "5000"     # The port your Flask container runs on
        prometheus.io/path: "/metrics"  # The endpoint where metrics are exposed  
    spec:
      containers:
      - name: flask-app
        image: anummdocker/flask-devops-app:45078ce05e1c0c03f18921b58f1fcc6a9b6a4685
        ports:
        - containerPort: 5000

